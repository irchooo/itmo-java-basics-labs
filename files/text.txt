--> Лаба 10 (ура!)
*блин, остались еще треды :)
Написать:
    1. метод, который читает текстовый файл и возвращает его в виде списка строк;
    2. метод, который записывает в файл строку, переданную параметром;
    3. метод, который склеивает два текстовый файла один (используя решения 1 и 2);
    4. метод, который заменяет в файле все, кроме букв и цифр, на знак ‘$’.
